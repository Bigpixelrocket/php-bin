#!/usr/bin/env python3
import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parents[1]))
from autorelease.control import main

raise SystemExit(main(["capture-evidence", *sys.argv[1:]]))
